# G-Snipers 海外版 · 交接文档

路径（问「交接文档在哪里」就答这个）：**`docs/HANDOVER.md`**

运维数字（IP、证书、容器）以 **`docs/PRODUCTION.md`** 为准。  
对外定位以 **`docs/POSITIONING.md`** 为准：每周获客操作系统，不是监测仪。不按 16 周表施工。  
机器、仓库、发版步骤有变：先改 `PRODUCTION.md`，再改这份清单。

约定：家里和公司**不会同时改**。换机器前必须把这边 push 完。

最后更新：2026-08-28 01:10（家里。§4.55 人测过。§4.56 客户说改完了。）。

---

## 0. 两台电脑

| 地方 | 本机副本 | 怎么用 |
| --- | --- | --- |
| 公司 | `E:\G-snipers海外版` | 默认就在这里打开 |
| 家里 | `D:\workspace\G-snipers海外版` | 回家在这里打开 |

盘符不同没有关系。Git 只认提交和远端，不认盘符。  
发版脚本也不绑盘符：在**当前仓库根目录**执行即可。

两台电脑都是独立 clone，通过 GitHub `origin` 会合。不要拷目录、不要 U 盘同步、不要互相覆盖。

---

## 1. 第一次在某台电脑下落仓库

只在该目录还不存在、或目录是空的时候做。

1. 打开终端，进入要放仓库的盘（公司 `E:\`，家里先确保有 `D:\workspace`）。
2. 克隆权威源（不要克隆镜像仓当主仓）：

```powershell
git clone https://github.com/caocan326-oss/g-snipers-global.git "E:\G-snipers海外版"
```

家里把路径改成 `D:\workspace\G-snipers海外版`。

3. 进入仓库，加上镜像远端：

```powershell
cd <本机仓库根目录>
git remote add upstream https://github.com/caocan326-oss/g-snipers-overseas.git
git remote -v
```

必须看到：

```
origin     https://github.com/caocan326-oss/g-snipers-global.git
upstream   https://github.com/caocan326-oss/g-snipers-overseas.git
```

4. 确认分支：

```powershell
git checkout main
git pull origin main
git status -sb
```

应是 `## main...origin/main`。

5. 本机 SSH（发版、登服务器才需要）。把 `deploy/ssh-config.example` 里的 `Host g-snipers-server` 拷进本机 `~/.ssh/config`。私钥用本机 `~/.ssh/g_snipers_deploy`，**不要提交私钥**。测一下：

```powershell
ssh g-snipers-server
```

6. 本机跑 Docker 时：`cp .env.example .env`，按需改本地值。`.env` 不进 git。

---

## 2. 每次开工（换机器后、当天开始写之前）

按顺序做，不要跳。

1. 打开**这一台**的仓库目录。
2. 读这份文件：`docs/HANDOVER.md`。
3. 读运维档案：`docs/PRODUCTION.md`（尤其是第 7、9 节）。
4. 看下面「当前状态」一节，确认上一台已经 push。
5. 对齐权威源：

```powershell
cd <本机仓库根目录>
git checkout main
git pull origin main
git status -sb
git log -1 --oneline
```

6. `git status` 必须干净（没有未提交改动）。若有上一台没推上来的本地文件，先停下来查，不要当成新需求继续改。
7. 不要 `git pull upstream`，除非你在修镜像。

做完这 7 步才能改代码。

---

## 3. 日常改代码

1. 只在 `main` 上改（当前没有用功能分支的约定）。
2. 本地验证（需要跑起来时）：

```powershell
docker compose up --build
```

演示登录（只用于演示租户）：`am@demo.gsnipers.com` / `demo1234`。

上第二个客户不要手插库。在仓库 `backend` 目录：

```powershell
python -m app.provision_tenant --name "客户名" --email am@customer.com --password "至少8位" --site https://www.customer.com
```

不要做公开注册页。

官网已经不是 snipers 时，容器启动 seed **不会**再灌门锁演示。它会把客户名改成跟域名走（`www.ugreen.com` → `UGREEN`），并清掉门锁问句/示例和门锁演示询盘（`alex@example.com`）。客户名已经是 **SNIPERS** 时，seed 和打开总览 / GEO / 客户说明 / 问题板 / 询盘也会清掉门锁问句和那条演示询盘，不会再灌回去；只有客户名仍是「演示客户 · 智能门锁出海」且官网还是 `snipers.com.cn` 时才留着演示。有抽查记录时必须先删 `geo_sample_results`，否则 backend 起不来（502）。

3. 改完在本机看 `git status`、`git diff`，确认没有把 `.env`、私钥、密钥加进去。
4. 用户明确要求提交时才 commit。不要主动 commit。
5. 提交后立刻推两个远端（镜像必须跟着权威源）：

```powershell
git push origin main
git push upstream main
```

6. 不要在 `upstream` 上单独 commit。不要 `git push` 到别的远程名。

---

## 4. 发版到生产

生产站点：https://www.weiyids.com  
生产目录：`/opt/g-snipers-overseas`  
SSH：`ssh g-snipers-server`（`root@39.97.52.149`）

**不要在服务器上 `git pull`。** 华北2 轻量访问 GitHub HTTPS 会卡住。  
**不要把 `/opt/g-snipers-global` 当发版目录。**

按顺序：

1. 本机确认在 `main`，工作区干净，且已经：

```powershell
git push origin main
git push upstream main
```

两个远端应是同一提交。

2. 在本机仓库根目录执行：

```powershell
powershell -File deploy/sync-from-local.ps1
```

改了 Dockerfile、依赖，或前后端业务代码时（镜像里 `COPY` 源码，不 Rebuild 容器仍跑旧包）：

```powershell
powershell -File deploy/sync-from-local.ps1 -Rebuild
```

3. 脚本会：打 `git bundle` → `scp` 到机器 → 服务器 `git fetch` 本地包 → checkout `main` → `docker compose up -d`。线上 `.env` 不在包里，不会被覆盖。
4. 浏览器打开 https://www.weiyids.com 看是否正常。
5. 需要时再 SSH 看容器：

```powershell
ssh g-snipers-server
cd /opt/g-snipers-overseas
docker compose ps
git log -1 --oneline
```

6. Nginx / 证书不在每次发版里动。改反代：先改服务器 `/etc/nginx/sites-available/g-snipers`，`nginx -t`，再 reload；然后把内容回写 `deploy/nginx/g-snipers.live.conf`，并改 `docs/PRODUCTION.md` 日期。

可选备份（发版前，在服务器上）：

```bash
tar -C /opt -czf /opt/g-snipers-backups/pre-$(date +%Y%m%d%H%M%S).tgz g-snipers-overseas --exclude=g-snipers-overseas/.git
```

这是代码 tar，同一块盘，不算客户库，也不算出机器。

---

## 4.1 客户库异地副本

诊断历史、Fact Pack、竞品记录只在生产这一台 Postgres。宿主机 5432 不映射。

| 地方 | 路径 | 说明 |
| --- | --- | --- |
| 生产本机落点 | `/opt/g-snipers-db-exports` | `BACKUP_HOST_DIR`。cron 每天 03:15 UTC 打一份 Postgres 自定义格式 |
| 生产 cron | `/etc/cron.d/g-snipers-backup` | 2026-08-22 已装。日志 `/var/log/g-snipers-backup.log` |
| 生产脚本 | `/opt/g-snipers-overseas/deploy/backup-postgres.sh` | 本机先写 dump，再按 `BACKUP_OFFSITE_*` 抄走 |
| 异地（家里） | `D:\workspace\g-snipers-db-offsite\` | **在仓库外面**，不要进 git |
| 异地（公司，拉过才有） | `E:\g-snipers-db-offsite\` | `pull-db-backup.ps1` 写在 clone 的上一级 |
| 管理员页 | `/ops/backup` | 手导出 JSON.gz 并下载。定时开关只是状态，真正跑的是 cron |

`BACKUP_OFFSITE_KIND` 仍是 `none`。没有第二台可 scp 的云。cron **不会**自动推到机器外面。出机器靠本机拉：

```powershell
powershell -File deploy/pull-db-backup.ps1
```

第一次已核对（2026-08-22，家里）：

| 项 | 值 |
| --- | --- |
| 文件 | `gsnipers-db-20260821-173000.dump` |
| 大小 | 398895 字节（约 390KB） |
| SHA256 | `e1e2e7ead5371a3287f37aa7e194795a7c49b5f0e428616f6ecb551b03c54ff1` |
| 服务器 | `/opt/g-snipers-db-exports/gsnipers-db-20260821-173000.dump` |
| 家里 | `D:\workspace\g-snipers-db-offsite\gsnipers-db-20260821-173000.dump` |

两边哈希一致，才算到了这台机器外面。公司那台如果还没有这份，在公司仓库根目录再跑一次 `pull-db-backup.ps1`。

不要把 dump、`.json.gz` 提交进 git。数字细节也可以看 `docs/PRODUCTION.md` 第 7 节硬性规则第 6 条。

---

## 4.2 探活（UptimeRobot）

不自建。托管盯 `https://www.weiyids.com/api/health`。2026-08-22 用官方 agent 接口提交，**邮箱已确认，监控已在跑**。

| 项 | 值 |
| --- | --- |
| 地址 | `https://www.weiyids.com/api/health` |
| 账号邮箱 / 告警 | `caocan326@gmail.com` |
| 周期 | 免费档约 5 分钟 |
| 微信 | 没有原生通道。手机开 Gmail 推送，或后台加 Telegram |

挂了、恢复都会发这封邮箱。后台：https://uptimerobot.com/

---

## 4.3 联网抽查源（百炼）

密钥 `DASHSCOPE_API_KEY` 仍在生产 `.env`，可以「只测这一源」。

「已配置的联网源都测」默认只跑 **博查 + Tavily**。百炼能带回网址，但海外问句经常给到国内论坛/站，不当默认证据。单测时不要再加 `search_strategy=agent`（非流式会 400）。

DeepSeek 仍只做分析，不算联网引用。

启动 seed 必须可重复跑。缺问句时不能再插第二条 `cite_checklist`，否则 backend 起不来（502）。

---

## 4.4 绿联实走后的身份

生产当前官网是 `https://www.ugreen.com`。`038c770` 起：

- 登录和 `/` 进 `/home`，不再进分发。
- 换站或 seed 会把演示门锁客户名改成跟域名走。
- 用量：博查/改法等外部调用**成功后再记账并立刻提交**，超时不会把已用滚回 0。顶栏写「今天已用：0/24」。
- 写出改法按钮旁有「已写 / 还剩」。
- GEO 来源卡不再印 `BOCHA_API_KEY` 这类键名。
- PDF 渲染失败返回 503 和原因，不再是无说明的 500。GEO 下 PDF / 检查记录失败会写在页面上。

IndexNow / GSC 同步灰着是没配钥匙和授权，不要当故障改。

---

## 4.5 同一问三处对齐

绿联实走后断在文案，不在按钮。同一条问：

`Which brand makes the best 100W USB-C charger for laptops?`

GEO 写全了；清单和客户说明曾截成「Which brand makes the best…」，标题停在「没提到我们」，摘要/复测已经写成「提到了」。Tavily 提到、博查未提到时，汇总曾写成「1 / 1 抽查里被提到」。卡片「被提到 尚未检查」看的是 8 个引擎空位，不是这一轮联网记录。

`46ac4e7` 起：

- 清单、客户说明、执行清单写完整问句，不再 28 字截断。
- 打开这三处时，未关闭项的标题按**这一轮**抽查改。有的源提到、都没官网 → 「提到了品牌，但没给出官网」。
- 同一点击里的博查 + Tavily 算一轮：`1 / 2`，并写明谁提到、谁没有。
- 卡片「被提到」按联网记录算。没手工打开的引擎空位仍是尚未检查。
- 外来网址仍不能算官网。工作台「人工上线」仍不是客户官网已经改完。

`a6eebaf` 起清单也写这一轮 `1 / 2` 和源名。源名用 **Tavily** / **博查**，不用小写 `tavily` / `bocha`。标题原文是「提到了品牌，但没给出官网」，不是五个字的缩写。官网来源已核对仍是尚未检查，直到有人打开网址。

博查常给中文电商、没有 ugreen.com，是源质量，不要当成按钮坏了。

---

## 4.6 客户能改的一页

`0efc59a` 起：待处理项写出改哪一页、官网地址、买家原问、站外发哪张卡。进度分三档：已写改法 / 已发给客户 / 客户已上线。第三档才允许验收和再测。「客户已上线」是客户经理登记，不是官网已改的证明。英文问里博查加一句：按中文网页看，不写成海外 AI 结论。

绿联实走（admin 进 ugreen.com 工作台）：100W 问已指到 `/products/usa-65585`，不是首页。客户说明「这周改三处」曾被两条 agents.md 和 500W schema 占满。`5e10164` 起至少留一格给这条 GEO 改法；改回「已写改法」时角标回到待处理。

`6fb37e2` 起：打开清单 / GEO / 客户说明就会按进度改角标，不必再点一遍按钮。进度「已写改法」而库里还留着上次 `verify` 时，刚进页也会回到待处理。清单和 GEO 同一状态用同一套词：待处理 / 进行中 / 待复查。不要把清单的「待核对」再当成这条工单的角标。

`cb78ec8` 起：客户说明有「发给客户的短稿」和「复制给客户」。GEO 待处理项也能复制单条。短稿只写客户要改的页、链接和站外卡，不写「工作台打勾」。PDF 和工作台三处仍留给客户经理看。

`01d8b3b` 起：第三档必须先填客户页或帖的 http(s) 地址。GEO、问题板、客户说明会显示这条链接。没有地址不能记「客户已上线」，也不能验收。登记地址仍不是我们打开核对过的证明。换到真实官网时，seed 会清掉 `old-blog.example`、`smarthome-weekly.example` 和门锁竞品（August Home / Level Lock / Qrio / Nuki）等演示站外示例，并去掉挂在这些缺口上的跟进。`.gitignore` 忽略 `backend/localtest.db`，本机 SQLite 顶替库不要提交。

---

## 4.7 人工复测后的修补

绿联实走后，老板单独走「老外能不能搜到 100W USB-C charger」会断在：生成买家问题 0 条、博查/Tavily 约 40 秒后 0 条且用量仍 0、排名绿钮空词空等、总览数字对不上、长操作无等待、客户说明漏 `GEO-ENT-002` / `schema`。

`432fd6a` 起：

- GEO 博查 / Tavily / 百炼和改法记账改为显式传租户，缺租户不调厂商。成功才记账。
- 买家问题 0 条用红字，提示先在总览保存关键词。空抽查也写清没有可检查的问题。
- 换站占位符跟当前站（绿联 `100W USB-C charger`）。空词排名先检查有没有词，绿钮只表示源已配置。
- 「生成处理建议 / 生成改法」和「载入常用渠道」有进行中文案；成功提示在页顶。
- 总览三块数字口径不同：左边只算网站紧急/优先。右边后来不再用「三处未关闭加总」当交付数，改成这周项，见 §4.32。不强行合成一个数。
- 新检查不再写入内部编号。客户说明和工作台把旧 `GEO-ENT-*` / `schema` 转成白话。空登录提示中文。

Tavily 国家过滤和空结果说明公司已先发（`cb78ec8` 一带），这次没改。IndexNow / GSC 灰着仍是没配钥匙。博查给中文电商仍是源质量。数字三套口径加了说明，不合成一个数。

---

## 4.8 意外失败用中文兜底

有人用本机 SQLite 顶替 Postgres 全站点过一遍。SQLite `database is locked`、进程崩，是顶替方案的代价，生产 Postgres 没有这个单写锁，不当产品缺陷。

真问题是：换站自动抓取失败、登录页在后端出问题时，界面原样显示英文 `Internal Server Error`。`api.ts` 把 `detail` / `statusText` 塞进页面，home / 登录都是 `setError(e.message)`。测速/排名有 `explainServiceError`，换站和登录没有。生产上任意一次 500 都会在这两个核心动作上甩技术黑话。

`526c158` 起：

- 后端未捕获异常记日志，接口只回「这次没办成，请再试一次。系统没有悄悄做完。」
- 前端 `api()` / 下载对 5xx、`Internal Server Error`、`database is locked`、连不上也收成这句。
- 已经写好的中文 4xx（空登录、缺关键词、上线缺地址）仍原样显示。

`e93dbf6` 补完尾巴：

- 完全连不上（没有 HTTP 状态）用「现在连不上服务，不是你的网络问题，请稍后再试。」后端 500 仍是「这次没办成…」。已经译过的中文不再包一层。
- 换站自动抓取失败只留「已保存。自动抓取没跑成…」，不再同时甩原始错误。
- `/api/auth/me` 未登录 / 登录已失效 / 用户不存在才去登录。服务挂了或连不上时页顶横幅「服务暂时不可用，请刷新页面重试。」，不清 token。

本地 SQLite 顶替不要当生产复现环境。人测里占位符、买家问题 0 条说明、绿钮空等、用量，以 `432fd6a` 和 www.weiyids.com 上的再走为准。

---

## 4.9 购物页不算官网；工单跟海外源

博查常给京东 / 天猫 / Amazon 等店链。以前只分「客户域名 / 其它」，店链进外来网址，页面上看不出为什么这次本来就不会给出官网。

`e93dbf6` 起：每条链接按规则分成官网、购物页、其它。购物页永不算「给出官网」。客户如果把店登记成官网，仍认官网。旧抽查不用重跑，读出来时按同一套规则拆。批次卡拆成：疑似官网 / 购物页（不算官网） / 其它外来网址。

`e1d2435` 起工单和复测吃进这套判断：

- 「提到了品牌，但没给出官网」只在 **海外联网源**（当前排除博查）提到、且没有客户官网时开。只有博查提到、全是店链 → 不开这张单。
- 待处理项理由写清分源（如 Tavily 提到 / 博查 未提到），不再写「回答里出现了外来网址」让人去对付店链。
- 复测只比海外源有没有提到、有没有客户官网。博查店链条数变多，不写成「这次提到了」。

`363b37b` 起「给出了官网」要人对着 **客户官网链接** 打开核对：

- 批次卡上疑似官网可点「打开过，核对通过」或「打不开 / 不是客户页」。只能选 owned URL；购物页勾会失败。
- 页头写明：默认抽查是 Tavily / 博查，不是 ChatGPT 本人。
- 客户说明：未核对应写「还要人工打开核对，不能写成已经确认」；有已核对比例才写出比例。

人工验收（生产绿联）：英文 100W 问跑一轮 → **只有本轮链接里出现 ugreen.com 时**才能测「核对通过」；没有客户官网链接时按钮不出现算正确，不要当成坏了 → 有购物页时京东勾应失败 → 客户说明未核对应诚实 → 待处理项只跟海外源 → 上线 URL 后再测只记变化。登记的上线地址 ≠ 抽查给出了官网。

`d905cbf` 起：没给出官网的短稿写清「搜索提到了品牌但没给官网链接 → 页上补英文事实和可点官网链 → 改完同一问再测，不保证这次给出官网」。批次卡在提到了但没有客户官网时，写明没有核对按钮是正常的。

### 4.10 站内短稿与 GEO 办事卡（`c2ea32a`，收口补丁见下）

站内紧急/优先项：问题板与客户说明「这周改三处」用同一套短稿（哪一页 / URL / 请做 / 怎么验），可复制。我们不代改官网。

GEO 默认进「待处理」：一张卡只留结果、改哪页、短稿、登记上线地址、同一问再测。源下拉 /「只测这一源」/ JSON / 空引擎 8 格收到客户经理工具或不再摊开。打开非演示租户时清门锁演示引用材料。

站外仍不进交付清单。

**生产绿联人测（2026-08-24，不跑新抽查、不改官网）**

| 块 | 结果 |
| --- | --- |
| A 待处理卡 | **过**（A1–A7；A8 同一问再测未测） |
| B 运营杂讯 | **过**（源下拉藏起；空 8 格不摊开；门锁稿已清） |
| C 站内短稿 | **初测 C2 不过**，后发 `6025d78`。2026-08-24 公司复看 GEO 黄框末行已有「不代改」。 |
| D 交付口径 | **过** |

A 要点：默认「待处理（办事）」；100W 卡有问句、1/2 提到（Tavily 提到 / 博查未提到）、请改 usa-65585、短稿；按钮仅复制短稿 / 登记已上线 / 同一问再测；无 JSON；空地址登记与再测灰掉；填 usa-65585 后显示「登记≠我们打开核对过」。

### 4.11 短稿卡内必须带「不代改」（`6025d78`）

人测后约定：站内 / GEO **卡片上展示的短稿**末尾都要有「不代改」，不能只写在客户说明总收口或只出现在复制串里。

- 站内：`我们不代改官网。改完告诉我，我再打开该页核对。`
- GEO：`我们不代改官网、不代发。改完告诉我，我再用同一问看一次。不保证这次被提到。`

复测：硬刷新后看 agents.md 黄框末行、100W GEO 短稿末行是否都有「不代改」。

### 4.12 站外一篇稿接到 GEO（`6e09125`）

GEO 待处理卡增加「站外这一条」：一篇英文发帖稿（只引买家原问和官网链接）、复制、打开官方发帖页、发完回填帖子链接。我们不代发、不代登、不群发。记下帖子不会自动变成「客户已上线」。刷新后帖子链接还在。站外仍不进问题板。

**生产绿联人测（2026-08-24 公司，不跑新抽查、不改官网、不真发）过。** 100W 卡有「站外这一条」、渠道 LinkedIn Company Page、两行英文稿、打开 `https://www.linkedin.com/company/` 后关掉。空链接不能记。记下 `https://www.linkedin.com/feed/update/test-ugreen-100w` 后刷新还在，写「登记≠我们代发」。处理清单未关闭仍 297，没有多出站外交付。角标本来就是「待复查」（这张卡早先已再测过），回填前后没变，也没变成已上线——这是对的，不要为了字面改成待处理。黄框末行有「不代改」。

### 4.13 发给客户的短稿带上站外英文稿

客户经理贴微信 / 邮件的那一条，要把「改哪一页」和「自己发这一条」合成一段。我们不代改官网、不代发 LinkedIn。

- GEO「复制短稿」和客户说明「复制给客户」都会带上：`请在「LinkedIn Company Page」自己发这一条（我们不代发）：` + 两行英文（买家原问 + 官网链接）。
- 黄框仍只写站内改法，不把「站外这一条」再抄一遍（卡上已有那一块）。
- 英文稿不编参数、不写「最好」「官方推荐」。站外仍不进问题板。

**生产绿联人测（2026-08-24 公司，硬刷新，不抽查、不改官网、不真发）过。** 页头 UGREEN / ugreen.com。

- 客户说明过：短稿第 3 条同时有「请改这一页」`/products/usa-65585`、官网链接、「请在「LinkedIn Company Page」自己发这一条（我们不代发）：」、两行英文（原问 + Official page）、收口「不代改 / 不代发 / 不保证这次被提到」。没有「工作台打勾」，英文没编规格。点「复制给客户」toast「短稿已复制」。这台读不到剪贴板，按页面原文过，不对粘贴字。
- GEO 100W 卡过：黄框末行有「不代改」；黄框里没有整段 `Buyers are asking`（那两行只在「站外这一条」）。点「复制短稿」按钮变「已复制」。按设计复制串应合成改页 + 自己发 + 英文两行；这台读不到剪贴板，不把粘贴当缺口去改。
- 处理清单过：未关闭仍 297，没有多出一条 LinkedIn / 站外交付。

### 4.14 历史网站恢复要写回客户名（`b98325b`）

从「历史网站」恢复时，以前只写回官网和工单，页头客户名可能还停在上一个站（生产上切到 gsnipers 再恢复绿联，官网已是 ugreen.com，名字还是 GSNIPERS）。现在恢复时按域名写回客户名（ugreen.com → UGREEN）。工单仍按站整包切换，不串。

站外工作台已走过 LinkedIn：事实包、英文短稿、记下要发（缺账号受阻）、测试帖回填。处理清单仍 297。不代发。回头人测对着这三块看即可。

### 4.15 站外卡片不说「可发」（`d7f02bf`）

人测前会误会「点一下就发出去」。渠道卡和页头改成：打开官方发帖页、客户自己登号或用自己的接口。不再写「接口可发」「去官网发」。缺账号仍受阻。不代发。

**生产绿联人测（2026-08-24 12:11，硬刷新，不登号、不真发）**

- LinkedIn 卡过：按钮是 AI 写一篇 / 记下要发 / 打开官方发帖页 / 自己的接口说明。没有「去官网发」「接口可发」。打开官方发帖页指向 `https://www.linkedin.com/company/`。卡上仍有标签「可接客户自己的官方接口」（不是按钮）。
- 对外稿过：走查短稿有买家原问和 usa-65585；「LinkedIn Company Page 发布稿」多 Learn more / Keywords，没编 100W 规格。
- 执行记录**初测不过**：预期回填后仍是「缺账号受阻」，实际是「已提交」+ 核验 failed（HTTP 451），渠道写成 directory。回填后不应再说我们提交了，也不该显示 directory。451 对测试链是对的。
- 处理清单过：未关闭 297，没有多出站外交付。

### 4.16 执行记录回填不是「已提交」

回填帖链接 = 登记，不是我们往 LinkedIn 提交。角标「已提交」改成「已回填」。渠道显示平台名（LinkedIn Company Page），不写 `directory`。记下要发且没账号时仍是「缺账号受阻」。核验 451 写「登记≠我们代发」。

**生产绿联人测（2026-08-24 12:36，硬刷新执行记录，不登号、不真发）**

- 角标过：原文「已回填」，旁边「社媒发布计划」。没有「已提交」。
- 渠道过：原文「渠道：LinkedIn Company Page」。标题「在 LinkedIn Company Page 发一篇」。不是 directory。
- 核验**不过（少一句）**：有「核验：failed」和「URL 返回 HTTP 451，暂未通过存活核验。」任务行没有「登记≠我们代发」。页顶通用说明有「我们不代发、不代登」，不在这条任务上。
- 处理清单过：未关闭 297。

### 4.17 旧核验行也写出「登记≠我们代发」

发版前写下的 451 原文不会自己改。列表和任务行在回填/核验失败时补上「登记≠我们代发。」不改库、不重核验、不代发。

**生产绿联人测（2026-08-24 12:54，硬刷新执行记录，不点核验 URL，不登号、不真发）**

- 核验原话过：`URL 返回 HTTP 451，暂未通过存活核验。登记≠我们代发。`
- 处理清单过：未关闭 297。

站外三块（卡片 / 对外稿 / 执行记录）人测收工。

### 4.18 站外页能复制给客户，不代发（`1e91706`）

工作台多四件，都不发出去：

- 对外稿 / 渠道卡「复制给客户」：渠道名 + 已有英文稿 + 官方发帖页 + 我们不代发。空稿不能复制。
- 卡上不再写「可接 / 接口可发」。
- 「接口报文」只展示客户自己调的报文，`sent` 恒为 false。
- 「记下客户已自备接口」只记状态，不收、不存钥匙。多传 token 会被拒。

站外仍不进问题板。不代发、不代登。

### 4.19 核对公开档案，不登号（`011c912`）

渠道卡可填「公开主页 URL」，点「核对档案」：公开 GET 该页，看打不打得开、页上有没有当前官网域名。发帖入口（如 LinkedIn `/company/`）不能当主页。HTTP 451 写成登录墙/地区限制，不等于没有主页，也不等于代发。不注册、不代登、不进清单。

**生产 SNIPERS 人测（2026-08-24 14:42，硬刷新站外，不登号、不真发）**

- 页头过：当前客户 SNIPERS，主域 `https://www.snipers.com.cn`。不是绿联。
- 没拿网上同名 Sniper 公司页来填。没有 SNIPERS 的 LinkedIn / Facebook 公司页可给。
- LinkedIn 卡贴官网 `https://www.snipers.com.cn/` 后点「核对档案」**不过**：红条原话「这是官方发帖入口或站点首页，不是这家客户的公开主页。先填具体公司页/档案 URL。我们不猜、不注册、不代登。」没有「官网打得开 / 该渠道还没有档案」的结论，字段没存成已核验。
- 原因：空路径被当成发帖入口。官网首页不是 LinkedIn 公司页，但也不该当 400。

### 4.20 官网首页记成「该渠道还没有档案」

发帖入口（LinkedIn `/company/` 这类）仍 400。客户自己的官网首页改为 200：记下「这是客户官网，不是该渠道公司页。官网打得开。该渠道还没有可核的公开档案。不要拿别人的同名页来填。」不把官网存成渠道 `profile_url`，不标已核验。卡上角标「该渠道无公开档案」。我们不猜、不注册、不代登、不代发。

**生产 SNIPERS 人测（2026-08-26，硬刷新站外，不登号、不真发）**

- T1 过：LinkedIn 卡贴 `https://www.snipers.com.cn/`，记下官网结论，角标「该渠道无公开档案」。没记成已核档案。
- T5 过：Facebook 卡同一官网，原话是 Facebook Page，不是已核 Facebook 页。
- T4 过：按钮没有「去官网发 / 接口可发 / 可接」。打开官方发帖页后关掉。处理清单未关闭 110，没有多出站外项。空稿没有「复制给客户」。
- T2 **不过**：贴 `https://www.linkedin.com/company/` 仍回官网结论，不是「这是官方发帖入口」红条。角标仍是「该渠道无公开档案」。
- T3 **不过**：清空后再核，没有「先填公开主页 URL」红条，仍是上一轮官网结论。
- 原因：核对沿用了上一轮官网，空值和发帖入口走不到。

### 4.21 核对档案只认当前输入框

空字段和发帖入口不再回落到已存官网结论。空 → 400「先填这家客户的公开主页 URL。我们不猜、不注册、不代登。」发帖入口 → 400「这是官方发帖入口…」。官网首页仍记「该渠道还没有档案」。框里改过或清空后，按这次填的走。不代发。

**生产 SNIPERS 人测（2026-08-26 15:14，硬刷新站外，不登号、不真发）过。** 页头 SNIPERS / `https://www.snipers.com.cn`。

- T2 过：贴 `https://www.linkedin.com/company/`。顶上红条原话「这是官方发帖入口，不是这家客户在该渠道的公司页。先填具体公司页 URL。我们不猜、不注册、不代登。」角标「档案未对齐」（不是「该渠道无公开档案」）。卡上文案同上，没有官网那句，没有「页上有官网」。
- T3 过：框已清空（占位「该渠道的公司页 URL；官网首页会记成还没有档案」）。顶上红条原话「先填这家客户的公开主页 URL。我们不猜、不注册、不代登。」角标「档案未对齐」。没有官网结论，没有「该渠道无公开档案」。
- 处理清单未关闭测前 110、测后 110，受阻 0、待复测 0，没有多出站外项。T1 这轮没做。这项收口。

### 4.22 站内「这周给客户改三处」

123 条问题不能整包丢给客户。站内页顶出现「这周给客户改三处」：从紧急/优先里每页只挑一条，最多三页。短稿仍是哪一页 / URL / 请做 / 怎么验，末行「我们不代改官网。改完告诉我，我再打开该页核对。」客户说明里的站内项用同一套挑选。不代改官网，不编规格。

**生产 SNIPERS 人测（2026-08-26 16:08，硬刷新站内，不改官网、不切绿联）**

- T1 过：标题「这周给客户改三处」。3 张卡，都标紧急，路径不同。问题板 102/102 没摊进这块。第 2、3 张是同一篇中英页，字面过。
- T2 过：三处都是 snipers.com.cn，末行「我们不代改官网。改完告诉我，我再打开该页核对。」没有绿联、没有工作台打勾、没有编规格。
- T3 过：复制短稿 / 复制这三处提示都对。没读剪贴板。
- T4 过：打开第 1 处仍在站内，问题板展开同一条。
- T5 **不过**：客户说明形状对，但站内项是 `articlelist/cat_id/2`、`articlelist/cat_id/3`，站内卡是 `article_id/4`、`article_id/3`、`/en/.../3`。另有一条 GEO/首页 + LinkedIn，不算第四页。
- T6 过：处理清单未关闭 110，没代改。
- 原因：客户说明另算一套，GEO 还占槽；同秒写入时挑选不稳定。

### 4.23 客户说明的站内三处必须和站内卡同一套页

站内卡和客户说明共用同一查询、同一排序（含 id）。先写出这三页，GEO 可以另附一条，不许顶掉站内页。不代改。

**生产 SNIPERS 人测（2026-08-26 16:49，硬刷新，不改官网、不切绿联）过。** 页头 SNIPERS / `https://www.snipers.com.cn`。

- T5 过：站内三张卡是 `/snipers/article/articlelist/cat_id/3.html`、`/snipers/article/articlelist/cat_id/1.html`、`/en/Article/detail/article_id/4.html`。客户说明「这周带给客户改的三处」和「发给客户的短稿」路径一一对上。每条有「请改这一页」，末行「我们不代改官网。改完告诉我，我再打开该页核对。」`cat_id/2` 没了。`cat_id/3` 是这轮第 1 张卡，不是顶替。没摊开几十条。
- GEO 另附一条：买家问智能锁许可时没提到我们，指向官网首页 `/`，让客户自己在 LinkedIn Company Page 发。末行「我们不代改官网、不代发。改完告诉我，我再用同一问看一次。不保证这次被提到。」没挤掉三页，不算第四个站内页。这条是门锁演示残留，见 §4.24。
- 处理清单未关闭 110。没代改。站内「这周改三处」收口。

### 4.24 买家问题必须跟当前客户，门锁残留要从 SNIPERS 清掉

T5 过的时候，客户说明多了一条「买家问智能锁许可」。SNIPERS 做紧固件，不是智能锁。这条是演示租户留在 `snipers.com.cn` 上的。

原先 `adopt_live_site` 遇到 SNIPERS 主机直接返回，中文「智能锁」也不在清洗词里，客户说明又不走清洗。所以现网一直挂着。

现在：客户名已经不是「演示客户 · 智能门锁出海」时，打开总览 / GEO / 客户说明 / 问题板 / 询盘会清掉门锁买家问题、待处理项、门锁搜索词，以及门锁演示询盘（`alex@example.com` / 加州物业经理）。SNIPERS 自己的引用材料（含 `snipers.com.cn`）不动。seed 不会再往已改名的 SNIPERS 灌门锁问句或那条演示询盘。光写「许可」不算门锁，避免误伤出口许可类问题。空的 GEO 是实话，不要编 SNIPERS 买家问题。不代发。`/execution` 现称问题板；下面 T3 写的「未关闭 102」是当时问题板加总，不是这周要做完，见 §4.32。

**生产 SNIPERS 人测（2026-08-26 18:46，硬刷新，不改官网、不切绿联、不编问句）过。** 页头 SNIPERS / `https://www.snipers.com.cn`。

- T1 过：客户说明没有「智能锁」。GEO 附条没了，写的是「还没有买家问题，AI 搜索这一块只能写尚未开始。」站内三页仍是 `cat_id/3`、`cat_id/1`、`/en/Article/detail/article_id/4.html`。短稿三条都有「请改这一页」和「我们不代改官网。改完告诉我，我再打开该页核对。」总收口「我们不代改官网、不代发。改完告诉我，我再用同一问看一次。不保证这次被提到。」是短稿固定末行，不是第四条 GEO。
- T2 过：GEO 买家问题空着。待处理写「还没有待处理项。先抽查买家问题，系统会按「提到了 / 没给出官网」生成短稿。」没有智能锁 / smart lock / スマートロック。没手加。
- T3 过：「买家问智能锁许可」没了。未关闭 **102**（受阻 0 / 待复测 0），比 T5 的 110 少 8。处理清单同时算站内 + GEO + 站外。少的不只是那 1 条门锁 GEO，还有演示站外缺口（`*.example`、门锁竞品）。站内项还在。不是站内被误关。
- T4 过：没代改、没代发、没编买家问题。

空的 GEO 是实话。不要编 SNIPERS 买家问题。不要代发。

### 4.25 买家问题只采用已记原句，不编

「生成买家问题」以前会按搜索词套出口 B2B 模板，编出 branded/category/competitor/task 问句。SNIPERS 空 GEO 时一点就会长出不是客户原问的英文句。

现在按钮是「采用已记原句」。只拷总览/选题里已经记下的买家原句（问号、够长的句子）。搜索词不是问句，门锁残留不采用。没有原句就空着，并写明不会自己长出问题。手写一句仍可以。不代发。

**生产 SNIPERS 人测（2026-08-26 19:05，硬刷新，不切绿联、不编问句、不代发）过。** 页头 SNIPERS / `https://www.snipers.com.cn`。

- T1 过：按钮原文「采用已记原句」，没有「生成」。空着点红字：「总览或选题里有搜索词，但不是买家原句。不会编成问句。请把原句记下来，或在本页手写一句再抽查。 新增 0 条，跳过 0 条。当前买家问题 0 条。」买家问题仍空，没编出英文问句。页上还有「不编买家问题：只有总览或选题里记下的原句能采用，搜索词不会自己长出问句。」
- T2 过：买家问题页「还没有买家原句。不要编。可点「采用已记原句」，或在下面手写一句再抽查。只有搜索词时仍会空着。」
- T3 过：客户说明仍写「还没有买家问题，AI 搜索这一块只能写尚未开始。」站内三页仍是 `cat_id/3`、`cat_id/1`、`/en/Article/detail/article_id/4.html`。
- T4 过：没代发、没手加问句。

### 4.26 有真问句时联网抽查要记账；SNIPERS 三页客户说改完后核对

一周循环里还没踩实的是：真问句 → 联网抽查 → 用量 +1。不编 SNIPERS 问句。用绿联已有的 100W 句，测完必须切回 SNIPERS。

**生产（2026-08-26 19:37）过。** 问句没改：`Which brand makes the best 100W USB-C charger for laptops?`

- 抽查前今天已用：博查 0/24 · 百炼 0/24 · Tavily 0/24。
- 抽查后今天已用：博查 1/24 · 百炼 0/24 · Tavily 1/24。百炼没跑是对的（默认只跑博查 + Tavily）。
- 绿条原文：「已对 2 个联网源各抽一轮：博查 Web Search、Tavily。DeepSeek 没跑，不算给出官网。」
- 提到了：是（提及 100%）。给出 ugreen.com：没有（给出了官网 0.0%）。没有红字。
- 页头已回到 **当前客户：SNIPERS** / `https://www.snipers.com.cn`。没编问句、没动知识库、没代发、没停在绿联。

提到品牌 ≠ 给出官网。短稿应写没给出官网，不写成已经推荐。

**同一问再测（2026-08-26 19:46）过。** 问句仍是这一句。抽查前博查 1/24 · Tavily 1/24，抽查后博查 2/24 · Tavily 2/24。绿条仍是「已对 2 个联网源各抽一轮：博查 Web Search、Tavily。DeepSeek 没跑，不算给出官网。」提到了（100%），没给出 ugreen.com（0%）。没有红字。页头已回 SNIPERS。不用改代码。第二次仍没给出官网，短稿继续写没给出，不要写成这次稳定了。

客户（推广）说站内三页改完了。经理打开核对，不代改：

- `cat_id/3` **部分过**：HTTPS 200，最终网址就是这一页。标题仍是「知识百科 | 赛珀 - 数字化转型服务商」。搜索描述「赛珀知识百科：收录、排名、内容等 SEO 说明文章列表。」和列表正文对得上。JSON-LD 现文只有 `CollectionPage` 的 `name` + `url`，没有公司、产品、认证，谈不上和正文核对这些事实。描述过了；标记还没齐。
- `cat_id/1` **过**：HTTPS 200，最终网址就是这一页，最新资讯列表可读，没有证书/超时/空白。
- `/en/Article/detail/article_id/4.html` **过**：HTTPS 200，最终网址就是这一页，正文能读到「新网站该如何优化推广」那五条。

第 1 页还要客户补 JSON-LD 里的公司/产品/认证（须和正文一致）。我们不代改。不要把 102 条整包丢回去。

**客户回告（推广，2026-08-26 19:40）和经理核对一致。** 知识百科搜索说明已对上列表：「收录、排名、内容等 SEO 说明文章列表」。标题后半截「数字化转型服务商」和 JSON-LD 写在模板里，后台改不了。资讯页和英文那篇本来就能打开，没动正文。JSON-LD 他们也记下了，仍是只有名称和网址。

经理不要再催后台改 JSON-LD。能改的说明已经改完。标记要动模板，那是他们网站的事，我们不代改。工作台这条可记「受模板限制」，不要当成客户没理。受模板限制也要这周回结果：改完给链接，或写清卡在哪个权限。不要把 102 条整包丢回去。不要把活拆到下一周。

**从正文抠字段交给推广（2026-08-26 19:48），不代改、不编。** 经理从 `cat_id/3` 正文只抄得到这些：

- 公司：赛珀 / Snipers，数字化转型服务商。邮箱 `snipers@snipers.com.cn`。地址：上海市闵行区宜山路2000号利丰广场2号楼。
- 产品：G-Snipers（AI 智能获客，SEM / SEO / GEO）。
- 认证：正文没有 ISO、TÜV 或其它资质，只有页脚「闽ICP备15015062号-6」（备案，不是认证）。认证空着，不编。

已交给推广，让他们补进 JSON-LD（现仍是 CollectionPage 只有 name + url）。改完经理再打开核：公司和产品在不在标记里；认证书必须仍空。没有的字段不编。我们不代改。

**再核（2026-08-26 19:52）还没补上。** 现网标记原文仍是：

```json
{"@context":"https://schema.org","@type":"CollectionPage","name":"知识百科","url":"https://www.snipers.com.cn/snipers/article/articlelist/cat_id/3.html"}
```

公司、邮箱、地址、G-Snipers 都没有。认证仍空（对）。已收死推广：不要厦门成都、不要编规格、闽 ICP 不能写成认证。他们改完经理再打开核，过/不过把新标记原文记下。不代改。

**谁改、改什么（等推广，我们不代改）。** 改的是他们网站**模板**里的 JSON-LD，不是后台文章。页：`https://www.snipers.com.cn/snipers/article/articlelist/cat_id/3.html`。只许补正文里有的：公司赛珀 / Snipers、数字化转型服务商、邮箱 `snipers@snipers.com.cn`、地址上海市闵行区宜山路2000号利丰广场2号楼、产品 G-Snipers（AI 智能获客，SEM / SEO / GEO）。认证不写。不许厦门、成都、ISO、TÜV、编规格。经理改完硬刷新再核。

**经理再核（2026-08-27 09:44）不过。** 只核 `cat_id/3`，硬刷新。标记原文仍是：

```json
{"@context":"https://schema.org","@type":"CollectionPage","name":"知识百科","url":"https://www.snipers.com.cn/snipers/article/articlelist/cat_id/3.html"}
```

公司、邮箱、地址、G-Snipers 都没有。认证仍空（对）。`cat_id/1` 和英文那篇不重核。

**卡在他们网站权限，不是我们产品。** 后台文章分类只能改名称 / 关键字 / 描述，动不了 JSON-LD。这段写死在主题文件里，要 `139.129.99.194` 上 `/template/pc/snipers/` 的文件权限。推广说他们没有这权限。改法（上面「只许补」那几项）已写下，交给**有这台机文件权限的人**去改主题。`139.129.99.194` 是客户站，不是我们生产 `39.97.52.149`。我们不登那台机、不代改主题。工作台这条记「受模板限制」。不挡往下开发。有权限的人这周改完，经理再只打开这一页核标记。不要把活拆到下一周。

**经理再核（2026-08-27 10:32）过。** 只核 `cat_id/3`，硬刷新。不代改。标记原文：

```json
{"@context":"https://schema.org","@type":"CollectionPage","name":"知识百科 | 赛珀 - 数字化转型服务商","url":"https://www.snipers.com.cn/snipers/article/articlelist/cat_id/3.html","publisher":{"@type":"Organization","name":"赛珀 / Snipers","description":"数字化转型服务商","url":"https://www.snipers.com.cn/","email":"snipers@snipers.com.cn","address":{"@type":"PostalAddress","streetAddress":"上海市闵行区宜山路2000号利丰广场2号楼"}},"about":{"@type":"SoftwareApplication","name":"G-Snipers","description":"AI 智能获客，SEM / SEO / GEO"}}
```

公司名、说明、邮箱、地址一字不差。产品是 G-Snipers，没有多写规格。认证仍空，没有 ISO / TÜV / 备案。页名用了完整 title，不是单写「知识百科」。`cat_id/1` 和英文那篇不重核。工作台上若还记着这条受模板限制，取消。钉着的第 1 张不用再等主题。

### 4.27 有一份审查 diff 方向反了，不要按那份改代码

2026-08-26 有人用落后本地仓对 `origin/main` 跑了 8 路审查，把「远程已经修好的」读成「HEAD 新引入的破坏」。审查自己也收回了，结论作废。那份仓后来快进到 `15ff509`，**不是**我们这份 `origin/main`（现 `b3dd2c2`）。不要去「修」下面这些，公司仓已经是对的：

| 审查说被破坏 | 公司 `main` 现在 |
| --- | --- |
| `seed_prompt_panel` 又用模板编买家问题 | 只拷已记原句。门锁或非问句丢掉。来源 `recorded-original`。按钮「采用已记原句」。 |
| `adopt_live_site` 只看域名、条件放宽、测试删了 | 仍是 `租户名 == 演示客户 · 智能门锁出海` **且** SNIPERS 主机才跳过清洗。`backend/tests/test_site_identity.py` 还在。 |
| 门锁标记两处各用一份词 | 问句/工单一律 `LOCK_PROMPT_MARKERS`（6 个，含「智能锁」「智能门锁」）。`LOCK_ASSET_MARKERS` 只管引用材料。SNIPERS 也清门锁稿，只留只写了 `snipers.com.cn` 的真材料。 |
| 「登记≠我们代发」和测试删了 | `distribution.py` 的 `FILLBACK_NOT_SEND` 还在，核验失败也会补上。`test_distribution.py` 还断言这句。 |
| 迁移 `023_platform_profile_check.py` 删了会断 alembic | 文件还在，有 `downgrade()`。 |

「生成买家问题」循环里每条单独查竞品/市场：那是旧模板路径。现 `seed_prompt_panel` 不走 `_prompt_pack_candidates`。这个函数还留在 `geo/common.py`，没有调用方。不要再接到「采用已记原句」上。量小，不单开一轮改。

不要按那 8 路报告改产品。下一台先 `git pull origin main`，对照本仓，不要对照那份落后快照。

### 4.28 工作台可记「受模板限制」，这周三处换下一页

`cat_id/3` 的 JSON-LD 后台改不了。以前只能一直占着「这周改三处」，或点「本轮不改」看起来像客户没理。

现在经理点「记受模板限制」：不关单、不代改。问题板还在，这条进受阻。这周改三处和客户说明跳过这条，补下一页紧急/优先。可点「取消受模板限制」加回去。不要用「本轮不改」记这件事。不会自动给现网 `cat_id/3` 打上，要点一下。

**生产 SNIPERS 人测（2026-08-27 10:02，硬刷新，没切绿联、没代改、没点「本轮不改」）过。** 页头当前客户：SNIPERS。

- 记下绿字原文：「已记下受模板限制。不是客户没理。这周改三处会换下一页。我们不代改。」
- 这三处变成：`cat_id/1.html`、`/en/Article/detail/article_id/4.html`、`/snipers/Article/detail/article_id/5.html`（紧急，新上来的）。`cat_id/3` 不在这三处。客户说明同样没有 `cat_id/3`，是上面这三页。
- 处理清单：未关闭 **102**，受阻 **1**，待复测 **0**。这条在受阻，不是已关闭。理由原文：「受模板限制。后台改不了，要等有主题文件权限的人改。不是客户没理。我们不代改。」
- 问题板红字同上，有「取消受模板限制」。点取消后绿字：「已取消受模板限制。这条可以再进这周改三处。」`cat_id/3` 回到三处第 1 张。没点「本轮不改」。

`cat_id/3` 标记 10:32 已过，见上。不再等主题。工作台上若还记着受模板限制，取消。**不挡我们交付工作台。**

### 4.29 钉住这周三处；可记已发给客户

交付的是经理能走完一周，不是客户主题改没改。第 5 篇新紧急页会顶掉这三处，经理没法把一周做完。

现在可「钉住这三处」：新抓到的紧急页不会顶掉。受模板限制仍换下一页。可记「已发给客户」：不是官网已改，也不是我们代改；记一条会钉住当前这三处。客户说明复查段写已发给客户，不写已经改完。可取消钉住、取消已发。

**生产 SNIPERS 人测（2026-08-27 10:21，硬刷新，没代改、没编问句）过。** 页头当前客户：SNIPERS。

- 钉住前三处：`cat_id/3.html`、`cat_id/1.html`、`/en/Article/detail/article_id/4.html`。点「钉住这三处」，黄字：「已钉住。新抓到的页不会顶掉。」按钮变成「取消钉住」。再硬刷新仍是这三页，没被新紧急页顶掉。
- 第 1 张点「已发给客户」。绿字原文：「已记下发给客户。不是官网已改，也不是我们代改。」块上还有「已发客户不是官网已改。我们不代改官网。」现在仍钉着，第 1 张是「取消已发」。

家里海外版仓仍是 `D:\workspace\G-snipers海外版`。`C:\Users\Administrator\gsnipers-website` 是另一个仓（他们 pull 到 `de45364`），不要当成这份 `g-snipers-global`。

### 4.30 这三处可「打开核对」

经理自己点「打开核对」：浏览器打开该页，工作台只记看过。不代改。**不会自己勾完，也不会从这三处拿掉。** 抓页若把状态写成已解决，要拨回去。拿掉过的可「放回刚拿掉的一页」。**不是等客户改完才能交付。** 客户改不改官网不挡工作台。`cat_id/3` JSON-LD 人眼 10:32 已过；它仍是这周第 1 张，不要因为工作台自己勾完就换页。总览也有同一按钮，见 §4.31、§4.36。

### 4.31 总览也看见这周三处

经理不用只进 `/onsite`。总览 `/home` 列出这三处和给客户的短稿。状态：待发给客户 / 已发给客户 / 打开过还没过。已钉住会标出来。可去客户说明、去站内。钉住、发给客户仍在站内。**打开核对在总览和站内都有。** 空的时候写「这周还没有要改的站内三处」。客户改不改官网不挡交付。我们不代改。

接口：`GET /api/dashboard/workbench` 多 `weekly_onsite`、`weekly_pinned`、`weekly_can_restore`、`this_week_onsite`、`this_week_open`。Fact Pack 没齐时 `next_actions` 第一条是 `fact-pack`；有三处时后面有 `weekly-three`。

### 4.32 问题板那一百多条不是这周要做完

总览第一屏不要再拿「未关闭 102」当交付数字。那是问题板检查记录加总。这周给客户看的是三处（外加 AI 搜索待处理）。

现网/本机对照：

- 总览第一格标题是「这周改三处」，写这周页数 + 可选 AI 搜索待处理，并写「问题板上的检查记录不是这周要做完」。
- 第三块标题是「这周交付」，主数是 `this_week_open`（站内三处 + 未关闭 GEO 工单）。内格是「站内三处」和「AI 搜索」。脚注写「问题板那一百多条不是这周要做完」。左边「网站检查」仍是紧急/优先加总，口径不同，对不上不是算错。
- 优先队列只列这周三处 + AI 搜索待处理，角标「这周 N 项」和「问题板 N 条，不是这周要做完」。
- `/execution` 标题是「问题板」。「未关闭」仍是 `total_open`，不改数字、不装成这周做完。

人测：登录后看总览第一格和第三块，不要看到一个像没做完的大盘数当主交付。再打开问题板，未关闭可以仍是一百多。客户改不改官网不挡。不要代改。已在仓库，**生产未发这轮**。

### 4.33 客户要看的是真问句有没有动

客户 KPI 是「老外问的时候有没有提到我们」，不是一周改了三页。经理从销售 / 询盘 / 展会 / 客户自己说记下原句。搜索词不要记，不要编。总览和客户说明列出这些问句：还没抽 / 这次提没提到 / 和上一轮对照。抽查不是 ChatGPT 本人。不保证这次被提到。没有原句就空着。

总览「AI 可见度作战室」可手录原句。`fastener` 这类搜索词红字拒：「这不是买家原句。搜索词不要记。不要编。」真问句记下后状态是「还没抽查」。演示租户（客户名仍是「演示客户 · 智能门锁出海」）可以有门锁问句；**SNIPERS / 已改名租户不要手加门锁问句，系统也不会再灌。** 已在仓库，**生产未发这轮总览手录框**（问句列表本身先前已有）。

### 4.34 AI 可见度作战室：时间序列 + 可引用资产

客户买的不是检查工具。同一批真问句要看见多轮趋势（提没提到 / 有没有官网）、AI 引用了谁。测完给出可粘贴英文段、FAQ、llms.txt，只写已记事实，缺的标 NEED_INPUT，不编规格。客户自己贴。我们不代改。不保证这次被提到。愿景全文见 `docs/VISION.md`。

### 4.35 已记问句常驻复测

按需手点不是监控。已记原句默认进常驻队列：从没抽过，或上次抽查超过 7 天，总览和作战室标「到期该复测」。经理点「复测到期问句」只抽这些已记句，不编问句，不自动烧配额。没有原句就空着。不保证这次被提到。我们不代改。

已在仓库。常驻复测是现增量里的「同一问再抽」，不是愿景里的全引擎作战室。没有原句不会编，不自动烧配额。总览「今天先做」在有到期句时出现「常驻复测：N 句到期」，按钮在 GEO。人测：没有原句时按钮是「没有到期问句」，灰着。有到期句只抽这些已记句。不要为了演示烧生产配额。

### 4.36 「打开核对」不能自己勾完

旧生产病：点第 1 张 `cat_id/3`「打开核对」后分析器写成已解决，从这三处拿掉。现行为：打开现网 + 只记看过。总览和站内都有按钮。抓页勾成已解决会拨回。可「放回刚拿掉的一页」，`cat_id/3` 回到第 1 张并保持已发给客户。我们不代改。

人测（不要改官网）：总览第 1 张点「打开核对」→ 可能弹出客户页，关掉即可 → 卡仍在这三处，状态「打开过，还没过」或仍是「已发给客户」，不是已解决。问题板已解决数不要因此 +1。若被拿掉，点「放回刚拿掉的一页」。已在仓库，**生产未发这轮总览按钮**（站内按钮先前已有）。

### 4.37 引用资产闭环

测完出草稿 → 已把这段发给客户 → 客户已贴上（要 http 页地址）→ 同一问再测。只写已记事实，缺的标 NEED_INPUT。没有原句就没有资产。工作台打勾不算官网已改。不保证这次被提到。我们不代改。

### 4.38 信任源地图 + 按问句/按轮对照

抽查里已经记下的引用和竞品名字，收成地图：客户官网 / 第三方 / 购物站 / 竞品站，以及同一问提到了谁。有两轮抽查时，对照上一轮和这一轮：提没提到、新出现/不再出现的站、新提到的竞品。总览作战室、GEO、客户说明「AI 引用了谁、提到了谁」共用这一份。没有原句或还没抽就空着。不会编来源、不编竞品。不保证这次被提到。我们不代改。

### 4.39 SNIPERS 也清门锁 llms.txt 草稿

人测：地图空着是对的。GEO「可供引用的材料」里 llms.txt 仍是演示客户智能门锁（中英日德），标草稿。历史网站里绿联是归档，没切过去。

现改：打开总览 / GEO 时，门锁/演示引用材料一并清掉。只写了 `snipers.com.cn` 的真材料不动。清完的 llms.txt 写成空草稿，不编 SNIPERS 页。按选题生成会跳过门锁选题。不代改。不编问句。

### 4.40 对外定位脊柱（不看 16 周）

收了 `docs/POSITIONING.md`。不按那份 16 周表施工。本阶段只认三道门：没有 Fact Pack（已批英文说明 + 官网）不出对外草稿；GEO 工单没有同一问复测不能关；本周三处只从紧急/优先未关闭项来，钉住是例外。不代改、不编问句、不保证这次被提到。

### 4.41 GEO 短稿跟客户说明同一份；没有可核渠道不写 LinkedIn

工单「复制短稿」带上 Fact Pack 的英文 FAQ / llms.txt，没有 Fact Pack 就写不能出对外草稿。没有已核公开主页的渠道不进短稿、不出现站外块。页标题去掉厦门/成都。GEO 待处理可走完：复制短稿（记下已发客户）→ 登记已上线 → 同一问再测 → 记下复测关掉。没有复测不能关。工作台打勾不是官网已改。

发给赛珀的本周需求在 `docs/snipers-week-brief-2026-08-27.txt`。四条已回，见 §4.42。

### 4.42 赛珀四条已回：FAQ 贴在 article_id/11；同一问复测仍没提到

2026-08-27 客户回：

| 项 | 回告 |
| --- | --- |
| 资讯页 | `https://www.snipers.com.cn/snipers/article/articlelist/cat_id/1.html` 还能打开。受模板限制，本周缩不了网址。 |
| 英文那篇 | `https://www.snipers.com.cn/en/Article/detail/article_id/4.html` 还能打开。受模板限制，本周缩不了网址。 |
| FAQ | `https://www.snipers.com.cn/snipers/Article/detail/article_id/11.html`（现成 G-Snipers 文末。首页后台改不了，没新开栏目。） |
| llms.txt | 做不到。根目录要主机文件权限。`https://www.snipers.com.cn/llms.txt` 现是 404。 |

经理核过：三问在该篇末尾，原正文没动。页脚本来就有厦门/成都，不是这次贴上去的。不要写进短稿。

工作台已记：三句 `cite_stage=published`，工单上线地址都是这篇。博查 + Tavily 同一问再测：三句都没提到，也没给出官网。工单已关，备注写清贴上了不等于被提到。资讯页、英文那篇「网址过深」记受模板限制。`cat_id/3` JSON-LD 先前已过，仍钉在这周三处。不代改、不编问句。

### 4.43 客户说明加英文周报给老板

不再切测试站、不再抽客户网站。客户说明页有「复制英文周报给老板」和英文 PDF。同一批已记事实：这周三处、同一问复测过/不过、这个月询盘。已关掉的复查仍写进英文周报，不藏「不过」。没有 Fact Pack 仍出周报，不出对外英文页草稿。不代改、不编问句。

2026-08-27 人测过：四项都在，绿字「英文周报已复制，可发给工厂老板。」三句原问关单备注都是没提到、没给出官网。询盘当时写 1 条，后清掉门锁演示，见 §4.45。

### 4.44 询盘可挂已记问句

登记询盘时可选本户已记买家问句；已有的条也能后挂。挂上不是证明 AI 提到了我们。没有原句就空着，不要编。客户说明中英都会写出挂了哪几句。总览「今天先做」在这个月有未挂问句的询盘时出现「这个月 N 条询盘还没挂问句」。门锁演示询盘 `alex@example.com` 不能出现在 SNIPERS，见 §4.24、§4.45。迁移 `026`。不开客户站、不再抽。

### 4.45 总览「今天先做」+ 门锁演示询盘不要回到 SNIPERS

`b2c9bc1` 已把 Fact Pack 排到工作台下一步第一位，并在打开 SNIPERS 时清掉门锁演示询盘。`45a21f1` 起总览第一屏 / 第三块 / 优先队列改成这周项，总览可手录原句，打开核对在总览也能点。旧总览第一格「网站风险」和大盘未关闭数不要再当现行文案。旧块名「优先处理队列」现为「今天先做」。`/execution` 现行标题是「问题板」，下面早期人测写的「处理清单」是当时页名。

现对照：

| 块 | 现在怎么写 |
| --- | --- |
| 总览第一格 | 「这周改三处」，不是「网站风险 102」 |
| 第三块 | 「这周交付」= 站内三处 + AI 搜索待处理。`this_week_open`。问题板加总只当说明 |
| 优先队列 | 只列这周项。角标「问题板 N 条，不是这周要做完」 |
| 今天先做 | 旧名「优先处理队列」。Fact Pack 没齐时第一条「先补 Fact Pack」。询盘没挂问句时有「这个月 N 条询盘还没挂问句」 |
| 问题板 | `/execution`。`total_open` 仍是加总 |
| 门锁询盘 | `alex@example.com` / 加州物业经理只留在演示租户。SNIPERS 打开总览 / 询盘会删掉 |

**租户注意：** 本机 seed 若客户名还是「演示客户 · 智能门锁出海」且官网是 `snipers.com.cn`，门锁问句和那条演示询盘会在，这是演示租户，不是 SNIPERS 生产。生产页头应是 **SNIPERS** / `https://www.snipers.com.cn`。不要用演示仓对照生产。

**生产走查 `526ed99`（2026-08-27 18:32，https://www.weiyids.com，不改官网、不代发、不编问句、没点抽查）7 条都过。** 页头是 SNIPERS，不是门锁演示。客户诊断目标里还留着 August Home / Level Lock / Qrio，没挡这 7 条。

1. 过：当前客户：SNIPERS，主域 `https://www.snipers.com.cn`。
2. 过：总览第一块「这周给客户改三处」，不是未关闭 102。第三块「本周期待处理优先级队列」这周 3 项，问题板 103 条不是这周要做完。
3. 过：`/execution` 标题「问题板」，未关闭 103。
4. 过：打开核对只记看过，三张卡还在。原文「只记看过，不是工作台勾完。还在这三处。我们不代改。」
5. 过：手录 `fastener sourcing` 被拒：「这不是买家原句。搜索词不要记。不要编。」
6. 过：今天先做第一件是这周改三处（页上 AI 分析建议已配置，所以没出现补 Fact Pack）。询盘这个月 0 条，没有 `alex@example.com`。
7. 过：GEO「复测到期问句」是灰的「没有到期问句」。只见已记三句。没点抽查，没烧配额。

### 4.45 总览提醒未挂问句的询盘

这个月有询盘还没挂已记问句时，总览「下一步」和动作条出现「去询盘」。挂上后这条消失。挂上不是证明被提到。不开客户站、不再抽。

### 4.46 门锁演示询盘从 SNIPERS 清掉

现网那 1 条是 seed 门锁演示（`alex@example.com` / 加州物业经理 / 多门锁批量安装），不是赛珀询盘。不要挂三句已记原句。打开总览 / 询盘 / 客户说明时按 `adopt_live_site` 清掉，不另编一条顶上。光写「批量安装」不算门锁。演示客户名仍挂 `snipers.com.cn` 时不动。

### 4.47 总览第一步补 Fact Pack

没有已批英文说明 + 官网时，总览「下一步」第一条是「先补 Fact Pack」，去站外内容页。补齐后这条消失。没有 Fact Pack 仍出周报，不出对外页草稿。不要编规格。不开客户站、不再抽。

### 4.48 客户资料收成 Fact Pack 草稿

经理把客户给的英文说明 / 说明书原文贴进站外内容页，点「从资料收成草稿」。只采用原文里的句子、网址、邮箱、写明的 ISO 等。中文不能当成已批英文。闽 ICP 不是认证。不打开官网。收成的是草稿，总览变成「核对并批准 Fact Pack」。批准后才能出对外页稿。门锁演示资料拒收。不要编规格。

**生产走查 `c84d173`（2026-08-27 19:16，硬刷新，不打开官网、不抽查、不批准）** 页头 SNIPERS。

1. 过：太短 → 「资料太短。把客户给的英文说明或说明书原文贴进来。不要编。」中文「我们是紧固件工厂，总部厦门，有认证。」同样拒。认证未填，无 ISO、无厦门。
2. 补测过（`1c2ab88`，2026-08-27 19:26，现网硬刷新）：页头「当前客户：SNIPERS」。贴「智能门锁适合租客安装」。红字原文「这是门锁演示资料，不能记到这个客户。不要编。」没有「太短」，没多出门锁草稿。未批那两份没动。
3. 过：页上已有客户英文收成草稿。官网 `https://www.snipers.com.cn`。认证空。GEO 仍挡对外稿。
4. 过：总览「核对并批准 Fact Pack / 草稿待批」。没点批准。
5. 空着（不批）。SNIPERS 现有**两份**未批草稿，先留着。不批准不出对外稿。不要清，除非用户说清。

### 4.49 活客户诊断目标清掉门锁竞品（`82dd632`）

打开总览 / 诊断目标，或保存目标时：不是「演示客户 · 智能门锁出海」的租户，去掉 August Home / Level Lock / Qrio / Nuki。再保存也写不回去。演示租户仍可留门锁竞品。不编问句、不批准草稿、不代改。

**生产人测（2026-08-27 22:30，硬刷新，没保存、没填门锁名）**

- T1 过（现网库里已删，当时过滤未发）：竞品列表空着，只有占位「对手名字」。没有 August Home / Level Lock / Qrio。灰字示意还是 smart lock，不是已存竞品。
- T4 过：页头 SNIPERS / `snipers.com.cn`。三句原句还在。未批草稿还在。没编第四句，没批准，没抽查。

**生产人测（2026-08-27 22:42，硬刷新，过滤已发）**

- T2 列表仍空：空着点保存，硬刷新后没有 August Home / Level Lock / Qrio。当时被「请至少填写 1 个核心关键词」挡住，整单没存上。
- T3 初测同样被关键词红字挡住，不算过滤过。

**生产人测 T3b（2026-08-27 22:54）过。** 关键词用了页上已有的 `GEO`。保存成功原文：「已保存目标：市场新增 0 / 更新 2，关键词新增 1，竞品新增 0。」硬刷新后列表里没有 August Home。没抽查、没批准、没开官网。

门锁竞品过滤收口。演示租户仍可留门锁竞品。

### 4.50 诊断目标灰字不要门锁示例

活客户（SNIPERS / ugreen 等）关键词占位不再用 `smart lock for renters`。只有客户名是「演示客户 · 智能门锁出海」才用门锁示例。SNIPERS 灰字应是 `SNIPERS official website` / `SNIPERS review`。灰字仍只是示例，不是已存词。不编买家问句。

页上已有关键词（如 T3b 存的 `GEO`）时，灰字被盖住。人测：硬刷新总览 → 诊断目标，**清空关键词框、不要保存**，确认没有 `smart lock` / スマートロック。不要填门锁名、不要批准、不要抽查。

**生产人测（2026-08-27 23:28，硬刷新，GEO 清空不保存，不填门锁、不批准、不抽查）过。** 页头 SNIPERS / `snipers.com.cn`。灰字原文「SNIPERS official website / SNIPERS review」，没有 smart lock、没有 スマートロック。竞品空着，占位「对手名字」。三句还在，未批草稿没动。没批准、没抽查、没开官网。

灰字刀收口。未保存，库里 T3b 的 `GEO` 还在，下次刷新可能回来，不要当已删。

### 4.51 没有客户英文，批准钮点不了

总览「去批准」改成「去核对」。草稿待批副文加上「没有客户确认过的英文不要批。」站外内容页「批准事实包」要先勾「这些英文是客户确认过的，不是我编的。」没勾不能批。不代批、不编规格、不清那两份草稿。客户英文还没到之前不要勾、不要批。

**生产人测（2026-08-27 23:53，硬刷新，没勾、没批）过。** T1–T3 过。没勾，没批。

这条收口。客户英文还没到：继续不勾、不批、不清草稿。

### 4.52 本周三处核对要记下过 / 不过

「打开核对」仍只打开现网、记下看过，不会自己勾完。看完经理点「记过」或「记不过」。过：`打开过该页。这一条现在对得上。不是我们改的。还在这三处。我们不代改。` 不过：`打开过该页。问题还在。还在这三处。我们不代改。` 还在这三处，不关单。没先打开核对不能记。记过/记不过不再抓官网。客户说明复查和英文周报会写上过/不过。不代改、不编问句、不批 Fact Pack。

**生产人测（2026-08-28 00:36，硬刷新。T2 起开官网只看不改。不批、不勾、不抽查）过。** T1–T5 过。没打开核对就点「记过」，红字「先打开核对这一页。只记看过还不算过。」打开核对后还在这三处，角标「打开过，还没过」。记过绿字「已记下核对过。这一条现在对得上。不是我们改的。还在这三处。我们不代改。」角标「核对过」。记不过「已记下核对不过。问题还在。还在这三处。我们不代改。」角标「核对不过」。两份草稿还在，三句还在。现网第 2 条核对过，第 3 条核对不过，三处还在。

这条收口。

### 4.53 今天先做要露出没过的本周三处

总览「今天先做」在本周三处已打开还没过、或核对不过时，出现「本周三处还有没过的」。现网应能看到第 3 条核对不过。没有已核公开主页时，工单短稿不再写出 LinkedIn。客户说明有「这周过了没有」，英文周报有 This week's pass / fail。不批 Fact Pack、不编问句、不代改、不再开官网。

**生产人测（2026-08-28 00:53，硬刷新。不批、不勾、不抽）过。** T1–T4 过。今天先做有「本周三处还有没过的」，1 处核对不过。三处还在，第 2 条核对过，第 3 条核对不过。客户说明有「这周过了没有」。两份草稿还在，三句还在。

这条收口。

### 4.54 过/不过要写进发给客户的短稿

角标「核对过 / 核对不过」不再只给经理看。核对过的短稿改成「这一页现在对得上」，不再写「请改这一页」。核对不过的短稿仍请改，并写「核对不过。问题还在。请再改。」整份「复制给客户」有没过的就起句「这周还有没过的，请再改」。总览三处卡也能单条复制。不批 Fact Pack、不编问句、不代改、不开官网。

`b026e6e` 已发。T1–T4 过时这条已在线上。

### 4.55 没过的再发给客户，不用去站内

总览三处卡能「记下已发 / 取消已发」。核对不过且还没记下已发时，今天先做改成「把没过的再发给客户」。已发给客户不是官网已改。我们不代改。不批、不勾、不抽、不开官网。

**生产人测（2026-08-28 01:08，硬刷新。不改官网。不批、不勾、不抽）过。** T1–T3 过。今天先做有「把没过的再发给客户」。第 3 条点「记下已发」后仍是核对不过，多了「已发给客户」。三处还在。没改官网。

这条收口。

### 4.56 客户说改完了，先记下来，先不开官网

已发给客户且还没过时，总览能点「客户说改完了」。不抓页、不改官网。今天先做改成「客户说改完了，去打开核对」。客户说了不算官网已改，还要再打开核对。不批、不勾、不抽。

---

## 5. 收工 / 换到另一台电脑之前

必须做完，另一台才能开工。

1. 该提交的已经 commit。
2. 推两个远端：

```powershell
git push origin main
git push upstream main
```

3. 本机再确认干净：

```powershell
git status -sb
git log -1 --oneline
```

4. 改这份文件的「当前状态」：写下日期、哪台机器、最新提交、做了什么、下一台要做什么。
5. 把「当前状态」的改动也 commit，并再 `git push origin main` 和 `git push upstream main`。
6. 不要留下未 push 的本地改动就关机/出门。

---

## 6. 当前状态

换机器前改这里。另一台开工先看这里。

| 项 | 值 |
| --- | --- |
| 日期 | 2026-08-28 01:10 |
| 最后一台 | **家里** `D:\workspace\G-snipers海外版` |
| 下一台 | 公司回去先 `git pull origin main`。不要两边同时改。 |
| 分支 | `main` |
| 产品提交 | 本提交：§4.56 客户说改完了。§4.55 `ac917ae` 已收口。 |
| 文档提交 | 本提交：§4.55 人测过 + §4.56。 |
| 已 push origin / upstream | 是（随本提交一起推） |
| 已发版生产 | 待本提交 Rebuild。`DEMO_LOGIN_ENABLED` 仍关。 |
| 本机工作区 | 提交并推完后应干净。 |
| 接口实测 | §4.55 T1–T3 过。现网第 3 条核对不过 + 已发给客户。 |
| 记下没挡 | SNIPERS **两份**未批 Fact Pack 草稿，没动。 |
| 仍做不到 / 不要做 | 网址过深本周做不到。llms.txt 做不到。贴上了不等于被提到。不要编问句、不要代改、不要再打开/抽客户网站、不要登 `139.129.99.194`、不要批准未核对的草稿、不要清草稿除非用户说清。 |

`www` 灰云、A 仍 `39.97.52.149`。`relay.weiyids.com` 橙云，不要 CNAME 回 `workers.dev`。不要开 Google Ads。不要在服务器 `git pull`。

### 6.1 回家第一件事（先对齐，再测，先不写功能）

两台不要同时改。公司这台已停。只打开家里那份 clone。

```powershell
cd D:\workspace\G-snipers海外版
git checkout main
git pull origin main
git status -sb
git log -1 --oneline
```

`git status` 必须干净。`git log -1` 应能看到本提交（门锁补测过入库）。不要 `git pull upstream`。

然后读本文 §6 和 `docs/PRODUCTION.md` 第 7、9 节。做完 §2 那 7 步才能改代码。

门锁补测已过，见 §4.48 / §6.2。家里不要重测这条。不要自己开新功能。用户说做哪件再做哪件。

### 6.2 门锁补测结果（现网，已过，不要重测）

站点：https://www.weiyids.com。2026-08-27 19:26。

- 页头原文：「当前客户：SNIPERS」
- 站外 → 对外稿。贴「智能门锁适合租客安装」。点「从资料收成草稿」。
- 红字原文：「这是门锁演示资料，不能记到这个客户。不要编。」
- 没有「太短」。没多出门锁草稿。未批那两份没动。

这条收口。不要再贴同一句测。不要批准。不要清那两份草稿。

### 6.3 不要动的产品事实

- 定位：每周获客 OS（`docs/POSITIONING.md`）。不按 16 周表。VISION 是天花板，不是今晚排期。
- 没有**已批准** Fact Pack（英文说明 + 官网）不能出对外页/FAQ/llms 草稿。草稿待批 ≠ 已批。周报可以没有 Fact Pack。
- 已记原句只有三句：什么获客软件比较好 / 什么GEO工具可以让我快速上去 / 有什么自动化的AI获客工具么？ 不要编第四句。
- FAQ 已贴 `article_id/11`。同一问复测（博查 + Tavily）仍没提到、没官网。工单已关：贴上了不等于被提到。
- 这周三处仍钉着；`cat_id/3` JSON-LD 人眼已过。深网址 / llms.txt = 受模板限制。
- 活租户是 `e18faac4` + `https://www.snipers.com.cn`。不要用那个官网写成 sanyglobal 的假 SNIPERS。
- 演示登录只用于演示租户：`am@demo.gsnipers.com` / `demo1234`。不要拿它对照生产 SNIPERS。

### 6.4 下一件（家里等用户开口，不要自己开）

§4.55 过。§4.56 随本提交发版：客户说改完了，先不开官网。现网第 3 条核对不过、已发给客户。客户英文还没到：不勾、不批、不清草稿。

---

## 7. 明确不要做的事

- 不要两边同时改。
- 不要在没 `git pull origin main` 之前开始写。
- 不要把没 push 的改动拷到另一台。
- 不要在服务器上 `git pull`。
- 不要提交 `.env`、`.env.bak-*`、私钥、真实密钥。
- 不要把 `/opt/g-snipers-global` 当发版目录。
- 不要从本机 rsync 整目录覆盖生产。
- 不要用 `git reset --hard` 除非你明确要丢掉那边的临时改动。
- 不要把 Postgres 的 5432 再映射到宿主机。
- 不要配置 Google Ads。
- 不要假设 `weiyids.com`（无 www）已经能开。正式地址是 https://www.weiyids.com 。
- 不要把客户库 dump / `.json.gz` 提交进 git。异地副本在仓库旁边的 `g-snipers-db-offsite\`，见 §4.1。
- 不要按 2026-08-26 那份 8 路审查改产品：diff 方向反了，见 §4.27。不要把「采用已记原句」接回模板编句，不要放宽 `adopt_live_site`，不要删「登记≠我们代发」，不要删 `023` 迁移。
- 不要登客户站 `139.129.99.194`，不要改 `/template/pc/snipers/`。JSON-LD 是他们主题，我们不代改。见 §4.26。
- 不要因为客户网站改不了就停交付工作台。记下受模板限制、钉住这三处、发给客户，一周照走。见 §4.28、§4.29。
- 不要把问题板「未关闭 102」当成这周没交付。那是检查记录加总。这周看总览三处和 AI 搜索待处理。见 §4.32、§4.45。
- 不要把门锁演示询盘 `alex@example.com` 当 SNIPERS 真线索。演示租户才留着。见 §4.24、§4.45。
